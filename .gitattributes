# به صورت پیش‌فرض همه فایل‌های متنی رو نرمالایز کن
* text=auto

# فایل‌های سورس‌کد همیشه به صورت LF ذخیره بشن
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.ts   text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# اسکریپت‌های سرور یا شل (برای لینوکس)
*.sh   text eol=lf

# فایل‌های ویندوزی (batch, cmd)
*.bat  text eol=crlf
*.cmd  text eol=crlf

# تصاویر و باینری‌ها نباید تغییر کنن
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.svg  binary
*.webp binary

# فونت‌ها
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary

# فایل‌های ساخت (build) و خروجی نرمالایز نمی‌شن
dist/* binary
build/* binary
